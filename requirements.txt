# proxymus-prive — runtime dependencies.
# Verified against Python 3.13 on Apple Silicon (the PoC target).
#
#   python3.13 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/python -m apf.proxy
#
# Versions are pinned to what the green test suite runs against.

# Proxy runtime
fastapi==0.136.1
uvicorn==0.47.0
httpx==0.28.1

# Detector ensemble.
# Production path (GLiNER + Presidio + regex) runs on `transformers` and is
# cross-platform. `mlx` / `mlx-lm` host optional/experimental generative
# adapters (Nemotron, Qwen3, Anonymizer) — only installable on Apple Silicon,
# pip markers skip them automatically on Linux / Windows / Intel Mac.
mlx==0.31.2 ; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm==0.31.3 ; sys_platform == "darwin" and platform_machine == "arm64"
transformers==5.1.0

# Test suite:  .venv/bin/python -m pytest apf/ benchmarks/
pytest==9.0.3
